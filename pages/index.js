import Head from "next/head";
const Home = () => (
  <div className="container">
    <Head>
      <title>Create Next App</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>

    <main className="bg-gray-400 h-64 w-full"></main>
  </div>
);

export default Home;
